export interface climberData {
    firstName: string;
    lastName: string;
    email: string;
    phoneNumber: string;
    dateOfBirth: Date | null;
  }

export interface loginData {
    usernameOrEmail: string,
    password: string
  }
